# Precision spindle

`model.ts` builds a belt-driven milling spindle cartridge in Three.js: seventeen parts
following a common layout — shaft with tool taper, back-to-back angular-contact nose pair
with spacers and lock nut, floating rear roller bearing, caps, drive pulley, and a drawbar
clamped by a disc-spring stack. Proportions are illustrative, in scene units.
